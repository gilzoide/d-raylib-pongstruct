module constants;

enum windowWidth = 800;
enum windowHeight = 600;
enum windowTitle = "PONGStruct";

enum ballVelocity = 300;

enum paddleWidth = 20;
enum paddleHeight = 100;
enum paddleVelocity = 400;
